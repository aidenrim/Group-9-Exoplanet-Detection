# Core astronomy data access
lightkurve>=2.4       # Download Kepler/TESS lightcurves from MAST
astropy>=5.3          # FITS file handling (used internally by lightkurve)

# HTTP + data
requests>=2.28        # Catalog download from NASA Exoplanet Archive TAP
pandas>=2.0           # Catalog parsing and manipulation
numpy>=1.24           # Numerical arrays

# Preprocessing
scipy>=1.10           # Savitzky-Golay detrending (Phase 2)

# Modeling
torch>=2.0            # CNN training (Phase 3)
scikit-learn>=1.3     # Metrics, train/test splitting

# Visualization
matplotlib>=3.7       # Plotting lightcurves and results
tqdm>=4.64            # Progress bars during training

# Web app
streamlit>=1.30       # Interactive frontend (app.py)
